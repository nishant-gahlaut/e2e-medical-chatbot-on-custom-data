from setuptools import find_packages, setup

setup(
    name="medical_chatbot",
    packages=find_packages(),
    version="0.0.0",
    description="medical chatbot using custom data",
    install_requires=[]
)
